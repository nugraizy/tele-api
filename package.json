{
  "name": "tele-api",
  "version": "1.0.0",
  "description": "",
  "main": "./dist/src/index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "gts lint",
    "clean": "gts clean",
    "compile": "tsc",
    "dev": "rimraf ./dist && NODE_ENV=development concurrently \"tsc-watch -p ./tsconfig.json --outDir ./dist --onSuccess 'node . --trace-sync-io'\"",
    "fix": "gts fix",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "posttest": "npm run lint"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.3.4",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "query-string": "^8.1.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.17",
    "@types/node": "^14.11.2",
    "concurrently": "^7.6.0",
    "gts": "^3.1.1",
    "tsc-watch": "^6.0.0",
    "typescript": "~4.7.0"
  }
}
